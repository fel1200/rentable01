import { Text, View } from "react-native";
import React, { Component } from "react";

export class ImageComplete extends Component {
  render() {
    return (
      <View>
        <Text>ImageComplete</Text>
      </View>
    );
  }
}

export default ImageComplete;
